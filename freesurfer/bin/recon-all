#!/bin/bash

# recon-all (wrapper)
# Matt Allbright
# This script is fed into by any commands calling recon-all.

#SBATCH --partition=long.q
#SBATCH --get-user-env
#SBATCH --ntasks=1
#SBATCH --ntasks-per-core=1
#SBATCH --output=/data/var/jobs/%j.out

echo "$@"
"$@"
